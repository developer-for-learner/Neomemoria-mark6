{
  "name": "neomemoria",
  "type": "module",
  "scripts": {
    "dev": "node tools/prepare-secure-build.mjs && node tools/patch-cloudflare-pbkdf2.mjs && vite",
    "build": "node tools/prepare-secure-build.mjs && node tools/patch-cloudflare-pbkdf2.mjs && vite build",
    "preview": "wrangler pages dev dist",
    "deploy:preview": "npm run build && wrangler pages deploy dist --project-name neomemoria --branch=security-ux-preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name neomemoria",
    "dev:sandbox": "node tools/prepare-secure-build.mjs && node tools/patch-cloudflare-pbkdf2.mjs && wrangler pages dev dist --d1=vocabflash-production --local --ip 0.0.0.0 --port 3000",
    "db:migrations:list:local": "wrangler d1 migrations list vocabflash-production --local",
    "db:migrations:list:prod": "wrangler d1 migrations list vocabflash-production --remote",
    "db:migrate:local": "wrangler d1 migrations apply vocabflash-production --local",
    "db:migrate:prod": "wrangler d1 migrations apply vocabflash-production --remote",
    "db:backup:prod": "node -e \"require('fs').mkdirSync('backups',{recursive:true})\" && wrangler d1 export vocabflash-production --remote --output=backups/vocabflash-production.sql",
    "db:verify:prod": "wrangler d1 execute vocabflash-production --remote --command \"SELECT name FROM sqlite_schema WHERE type='table' AND name='auth_sessions';\"",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.11.9"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.18.2",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}
